<%= form_for(@alcance) do |f| %>
  <% if @alcance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alcance.errors.count, "error") %> prohibited this alcance from being saved:</h2>

      <ul>
      <% @alcance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.text_field :usuario_id %>
  </div>
  <div class="field">
    <%= f.label :comuna_id %><br>
    <%= f.text_field :comuna_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
